/**
 * Render the triaged digest as 🟢/🟡/🔴 markdown.
 * Status: Phase 7 ports the real impl.
 */

import type { TriagedItem } from '../types';

export const renderDigestMarkdown = (triaged: readonly TriagedItem[]): string => {
  void triaged;
  return '';
};
