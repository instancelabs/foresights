/**
 * Mount the digest panel into the DOM with copy / download / close handlers.
 * Status: Phase 7 ports the real impl.
 */

import type { Deps } from '../types';

export const mountDigestPanel = (deps: Deps, markdown: string, productId: string): void => {
  void deps;
  void markdown;
  void productId;
};
