/**
 * Render the active-issues / RFCs section (label-derived status badges).
 * Status: Phase 5 ports the real impl.
 */

import type { Deps, FlagMeta, Issue } from '../types';

export const renderRfcs = (deps: Deps, issues: readonly Issue[], baseMeta: FlagMeta): void => {
  void deps;
  void issues;
  void baseMeta;
};
