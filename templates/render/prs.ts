/**
 * Render the recently-merged PRs section.
 * Status: Phase 5 ports the real impl.
 */

import type { Deps, FlagMeta, PullRequest } from '../types';

export const renderPrs = (deps: Deps, prs: readonly PullRequest[], baseMeta: FlagMeta): void => {
  void deps;
  void prs;
  void baseMeta;
};
