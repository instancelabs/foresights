/**
 * Render the live releases section.
 * Status: Phase 5 ports the real impl.
 */

import type { Deps, FlagMeta, Release } from '../types';

export const renderReleases = (
  deps: Deps,
  releases: readonly Release[],
  baseMeta: FlagMeta,
): void => {
  void deps;
  void releases;
  void baseMeta;
};
